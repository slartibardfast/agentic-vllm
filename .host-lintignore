# Paths excluded from `host-lint --all`: not this host's authored live content,
# so out of scope for the naming audit here.

# The Where room, and the gate tools: bare stores + worktrees under software/
# (see .host-software). Materialized locally from the recipe and gitignored;
# each is audited in its own repository, and --all walks the filesystem, so the
# room is named here.
software/

# The referenced methodology template: audited upstream, not host content.
host-template/

# Generated mdBook output.
book/
mdBook/

# The append-only record: MEMORY.md is frozen history, never reworded. It is
# excluded by construction whether or not it is listed; the entry makes the
# exclusion visible.
MEMORY.md

# Dated measurement artifacts: frozen records of benchmark and oracle runs.
plan/*/oracle-run-*.md
.zcode/
.weco/

# Session-scope tool state: the ZCode harness's plan-mode files and the weco
# optimizer's run stores. Machine-authored scratch, not this host's content.
.zcode/
.weco/

# The closed research campaign that fed plan/0007: outline, authored summary
# report, and the skill's generated JSON results (data artifacts, rewording
# them would falsify what the tools produced). Verdicts are recorded in the
# plan; the campaign is history.
research/

# Closed milestone bodies: frozen campaign records whose measurement
# numerals are data, not names. plan/0006 is the delivered record. The open
# milestones (0002 through 0005, 0007) stay audited; their data phrases are
# lexicon entries instead, because their READMEs carry the task graph the
# receipt gate reads.
plan/0006-cu-sm80-on-sm75/

# Dated measurement report inside an open milestone: record, not live prose.
# The plan README beside it stays audited.
plan/0002-turing-marlin-backend/d8-report.md
