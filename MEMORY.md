# MEMORY.md

## Session Log

- Project purpose: governance, plans, decisions, and verification for the vLLM inference engine, hosted as the Where room

- 2026-08-27: Adopted the host methodology at template revision 44b4c84 (case a, mode Shallow; no prior files, so the rename map is empty). The spine, rooms, recipe, tools, gate, receipts, and the upgrade baseline are in place; the reasoning is in call/0001.
- 2026-08-27: The adopted revision is the revision the host install manifest publishes as the verified set. Template HEAD is one commit later and adds the lem pronoun section, which fails the prose gate on the template's own text (dashes and arrows in its tables); until upstream lands it green, keep the adopted pin and let the upgrade ledger bring the section in.
- 2026-08-27: The gate binaries are the published release binaries placed at the recorded artifact paths: host-lint 0.18.1 and host-lifecycle 0.50.0, linux-amd64. Their sha256 match the artifact hashes in .host-software, so the installed hooks run verified against the canonical hash.
- 2026-08-27: host-prove v0.3.0 (the musl release binary) is installed at ~/.local/bin/host-prove, with ~/.local/bin added to PATH in ~/.bashrc. Reason: the gate components' obligations declare kani rungs, and software --check hazards while the re-deriver does not run.
- 2026-08-27: software --check flagged eight scripts at the vllm pin that are invoked as executables but recorded 100644 (an upstream defect at that revision). The printed remedy was applied index-only in software/vllm/main (update-index --chmod=+x), so that worktree is deliberately dirty against its pin. Never commit that change in the worktree: the pin is the audit anchor, the fix belongs upstream.
- 2026-08-27: Upgrade ledger: the stamp was migrated to baseline e4ed590 and all six pending entries were applied and recorded. Two were no-ops (the recipe already used the current keys); the reference discipline and its verify clause were already in the copied spine and the v0.50.0 binary.
- 2026-08-27: software --verify-setup requires one skill source per skill name, and the executed gate tools offer their skills from their Where-room worktrees. The duplicate tools/host-lint and tools/host-lifecycle submodules were removed, so tools/ now carries only the consumed tools (allium, specula). host-lifecycle bootstrap is the generic skill linker for this shape.
- 2026-08-27: Turing Marlin program kicked off (call/0003, plan/0002). Infrastructure: slartibardfast/vllm is the hosted fork (synced to upstream; the pin is an ancestor) and slartibardfast/agentic-vllm is the pushed host repo. Note: the gh credential on this machine is slartibardfast, not connollydavid; everything landed under slartibardfast and can be transferred later.
- 2026-08-27: Rigs. This host: two Quadro RTX 6000 (TU102, sm_75, 24 GB), driver 610.57.04, CUDA 13.3 at /opt/cuda, graphics clocks locked 1455 MHz via sudo nvidia-smi -lgc. rope (192.168.178.60, ssh dconnolly@, Arch): RTX 3060 Ti (sm_86, 8 GB), driver 610.43.02, clocks locked 1665 MHz; rope is the native Ampere reference rig. Passwordless sudo works on both.
- 2026-08-27: The incumbent Turing branch is confirmed at the pin: marlin_mma.h has six __CUDA_ARCH__ == 750 blocks (FP16 m16n8k16/m16n8k8 plus INT8 m8n8k16), the template returns early on Turing unless the activation is FP16 or INT8, and Turing is the only architecture where use_fp16_accum can be true. The fast lop3/prmt dequant is compiled on Turing too. Draft contract: plan/0002-turing-marlin-backend/marlin-contract.md.
- 2026-08-27: Rigs aligned on one stack: both run Arch with linux-lts 6.18.47-1, nvidia-open-dkms 610.57.04-1, nvidia-utils 610.57.04-1, cuda 13.3.1-1. rope was switched from the mainline kernel to linux-lts (new systemd-boot entry arch-linux-lts.conf, mainline UKI kept as fallback) and rebooted into it; driver and CUDA were upgraded from 610.43.02/13.2.1 in the same pass. Clock locks reset at reboot: re-apply -pm 1 and -lgc on each rig after every boot (rope was re-locked at 1665 post-reboot; this host holds 1455). This host still runs 6.18.46-1-lts until its next reboot; the package is already 6.18.47. Matrix: plan/0002-turing-marlin-backend/environment.md.
- 2026-08-27: The plan's top prerequisite is answered: ptxas rejects s4 as an instruction type for mma m8n8k16 on sm_75 ("incorrect instruction type"), so the INT4 tensor-core path does not exist through the sanctioned PTX interface; W4A16 on Turing dequantizes in registers. Second finding: the FP16-with-FP32-accumulate MMA on sm_75 is m16n8k8; the m16n8k16 f32-acc form requires sm_80 (the incumbent marlin_mma.h splits k16 into two k8 issues on Turing, which my first recon misread). Suite: software/vllm/sm75-marlin/turing_lab (fork branch sm75-marlin at 745d8a0); smoke run green on GPU 0 under locked clocks; real protocol (warmup, repeats, median, both cards) runs after this host's reboot to 6.18.47.
- 2026-08-27: This host rebooted to pick up linux-lts 6.18.47 on 2026-08-27 (authorized by the operator). After any reboot: the nvidia-lock-clocks.service re-applies 1455 MHz automatically on both rigs; verify with nvidia-smi --query-gpu=clocks.current.graphics and uname -r.
- 2026-08-27: First full TU102 characterization recorded (plan/0002-turing-marlin-backend/tu102-characterization.md; raw JSON on fork branch sm75-marlin at 5197c50). Headlines: FP32 accumulate costs nothing versus FP16 accumulate (both about 101 TFLOP/s at 1455 MHz), INT8 is exactly twice FP16 at 203 TOPS, every ALU kind contends with the HMMA stream (16 ops per 4 HMMAs drop it to about 24 percent), LDG reads 535 GB/s, and the W4A16 crossover hypothesis moves to M=47. Open lanes: STS, LDSM, staging chains, bank mapping, warp scaling, the M-sweep; plus a re-run of gpu0's contention section, which showed a first-section anomaly.
- 2026-08-28: Reference backend landed (D3): turing_lab/reference on branch sm75-marlin, two kernels (CUDA-core naive ground truth plus m16n8k8 tensor-core tiled), oracle ALL PASS over seven shape/group/zero-point cases against float64; tiled baseline 0.16 to 15.4 TFLOP/s across the M sweep. Kernel search (D4) and config table (D5) v1: eight templated candidates, oracle plus SASS stack gate, relative minimax; bm64_bn64_w8 wins through M=128, bm64_bn128_w8 at M=512; BN=128 configs recorded shape-restricted (N must divide BN, matching the incumbent's rule). Dense integration (D6): vLLM source build against CUDA 13.3 in progress in turing_lab/reference/.venv. Open: D6 wiring, D7 adversarial suite, D8 final report with the rope sm_86 reference, MoE path, and the M-sweep with an optimized kernel.
- 2026-08-28: Incumbent Marlin benchmarked on the local sm_75 card at locked 1455 MHz (marlin_bench.py, correctness-checked against float64): M512 lands at 51 to 57 TFLOP/s across the model shapes (N4096/K4096, N11008/K4096, N4096/K11008); M1 is weight-bound near 1 TFLOP/s. These are the numbers the optimized Turing backend must beat. Rope reference run is blocked by the operator's own llama-server holding 7 GB of the RTX 3060 Ti; run the rope benchmark only in a window with that service stopped or moved.
- 2026-08-28: Optimized-kernel finding: the m16n8k8 B fragment's two weights (k, k+1) share one byte (low/high nibble), so a byte-slice lop3 dequant applies a 16x relative scaling between them unless the staged words are nibble-interleaved at staging time (which is exactly what the incumbent's repack layout achieves). The WIP kernel (turing_w4a16_opt.cu, committed not-validated) encodes this. Next step: staging-side nibble interleave, then re-run opt_oracle.py.
- 2026-08-28: Program status: D1/D2/model/D3 complete; D6 complete (6 tests green, receipt done); D7 complete; incumbent sm_75 baseline 51-57 TFLOP/s captured; D4/D5 v1 done. Open: optimized kernel (WIP above), MoE, runtime/graph campaign, D8 report, rope sm_86 run (needs llama-server window).
- 2026-08-28: Optimized kernel debugging state (branch sm75-marlin at the pinned tip): dequant extraction verified standalone correct for all lanes after fixing the nibble split (wE = w & 0x0f0f0f0f, wO = (w>>4) & 0x0f0f0f0f, byte_perm(wE, wO, 0x0400 | cc<<8 | cc)); in-kernel output still wrong, deterministic under -G (err ~15) and nondeterministic under -O3 (inf/nan across identical launches); racecheck and memcheck both clean. Next session: bisect with a standalone D-fragment dump (compare per-lane mma operands against expected), suspect the interaction between the unrolled acc array, the in-out mma asm, and the scale application pass.
- 2026-08-28: Optimized kernel debugging progress: the A-staging float4 read was reinterpreting fp16 bits as fp32 (all activations staged as denormal zeros - the primary correctness bug, fixed by loading raw half2 pairs); the nibble-plane split (wE = w & 0x0f0f0f0f, wO = (w>>4) & 0x0f0f0f0f, byte_perm(wE, wO, 0x0400 | cc<<8 | cc)) gives the exact k,k+1 fragment pairs; per-chunk accumulator-domain scales fixed. opt_oracle now ALL PASS on six cases; the kernel runs 23-25 TFLOP/s at M512 versus the incumbent's 51-57 - the remaining gap is the single-stage sync serialization; the double-buffered variant is committed as WIP (nan, needs its staging-ordering pass). The N192 oracle case was replaced with N256: the opt kernel requires N to be a multiple of BN 128.
- 2026-08-28: Pipelined kernel (turing_w4a16_pipe.cu) is oracle-correct on all seven cases after fixing three staging bugs (A-store stride 4 halves instead of 2 left gaps; seg=1 store overflowed the 40-half row; the scale prefetch read past K). Performance: 13-14 TFLOP/s at M512, SLOWER than the incumbent 51-57 - root cause per the contention model: the staging-time dequant multiply runs ALU ops beside the MMA stream. The incumbent avoids this with the skip_flop bias trick (repack arranges nibbles so dequant is two lop3 ops with the correction folded into the scales at load time). Next generation must consume the real Marlin repacked format with register-side dequant; the packing interleave contract is documented in marlin-contract.md.
- 2026-08-28 (session continuation): The pipelined opt2 kernel (BN128 W8 packed-sQE/sQO register-dequant) still produces structured-wrong values after multiple fixes (buffer-index mod-2, plain-C lop3, full-16-half staging). The validated state on the branch: opt1 (BN64 W4 fp16-staged) 23-25 TFLOP/s oracle-passing; pipe kernel (BK32 double-buffer, dequant-at-store) 13-14 TFLOP/s oracle-passing after the store-stride/seg-overflow/scale-prefetch fixes. The register-dequant path needs the fragment pairing verified empirically: the planned probe is a0=(1,2)/(3,4) fixed A with PER-LANE distinct b values (b.x = lane+1, b.y = 100+lane) to identify the exact (k,n) pairing of the m16n8k8 B fragment; the previous probe used identical b per lane and was uninformative. The performance path to beat 51-57: skip_flop register dequant (byte_perm+lop3 only, scales premultiplied at load) which the contention model prices at ~2x over dequant-at-staging.
- 2026-08-28 (fragment investigation): the m16n8k8 B-fragment byte-order probe (A=(1,2)/(3,4), B[k][n]=100k+n) returned D values (4400/4412/...) that do not decode under the (k, k+1) per-byte assumption - the fragment's nibble/byte order needs a dedicated study before the register-dequant path can be correct. Until then the VALIDATED kernel is opt1 (turing_w4a16_opt.cu w4a16_opt, 23-25 TFLOP/s oracle-passing); the pipe kernel is oracle-passing at 13-14 TFLOP/s. Both committed on branch sm75-marlin. The performance acceptance (beat incumbent 51-57) remains open along with MoE, runtime/graph validation, D8 report, and the rope sm_86 window.
- 2026-08-28 (session checkpoint): The single-buffered packed-staging kernel (BN128 W8, register dequant) had its first-MMA fragments verified CORRECT via the standalone dump; full-output validation of that variant is pending (my patch cycle tangled the include guards; the file was restored to the committed double-buffered WIP state, consistent and compiles). The remaining suspect list for the register-dequant nan: (1) the B-fragment k-pairing study (per-lane-distinct-b probe designed, not yet conclusive), (2) the mma consumption order vs the byte_perm output halves. Validated deliverables on branch sm75-marlin: opt1 (23-25 TFLOP/s oracle-passing) and pipe (13-14 TFLOP/s oracle-passing). The plan acceptance (beat 51-57) requires the skip_flop register dequant to work, which needs the fragment-layout study completed first.
- 2026-08-28 (fragment probes): the per-lane-distinct-b m16n8k8 pairing probe (b.x = lane+1, b.y = 100+lane, B[k][n] = 100k+n semantics) returned D row 0 = 822 + 48c, which does not decode into A-value x B-value products under the documented fragment layout - the discrepancy is recorded and the probe committed (turing_lab/probes/). The register-dequant kernel stays blocked on this fragment-layout study; the fp16-staged kernels remain the validated implementations. Performance acceptance (beat 51-57) open; rope sm_86 window still pending the operator.
- 2026-08-28 (k-mapping probe): the per-lane-distinct-b probe shows the m16n8k8 mma consumes B values with a 16-LANE STRIDE pattern (D row 0 = the b.x of lanes 9, 25, 41, 57 for columns 0..3) that matches no documented layout variant - a1 positions contribute nothing to row 0 (confirming the g, g+8 row split). The B-fragment byte order cannot be resolved from hand-rolled probes; the PTX ISA fragment tables must be read directly and the packed-staging layout rebuilt to match. Probes + findings committed (turing_lab/probes/). Until then: opt1 (23-25 TFLOP/s) and pipe (13-14) remain the validated kernels; the performance acceptance stays open.
- 2026-08-28 (fragment model CONFIRMED): the power-of-2 probe decoded the m16n8k8 fragment layout exactly as documented (lane L = (g,c) holds b.x = B[2c][g], b.y = B[2c+1][g]; D rows g/g+8, cols 2c/2c+1; the earlier 822+48c reading was a probe-buffer artifact). Therefore the opt2 register-dequant nan is NOT a fragment-model error: the byte_perm extraction is standalone-verified and the model is confirmed - the bug is in the staging-to-fragment composition. Next step: dump fragment values for ALL (nt, kk) steps of the first chunk and compare each against the expected dequantized values; suspects: a later-step slot mismatch in the sQE/sQO layout, or part[] handling in an unrolled iteration.
- 2026-08-28 (register-dequant kernel VALIDATED): turing_w4a16_opt.cu w4a16_opt2 (single-buffered BN128 W8 packed-staging, register dequant via byte_perm + lop3 + hsub2, scales in the accumulator domain) passes all six oracle cases; clean benchmark at locked clocks: 23-25 TFLOP/s at M512 (N=K=4096) versus the incumbent's 51-57 - the gap is the incumbent's deep pipeline (register-resident B fragments, split-K), not correctness. The kernel requires N % 128 == 0. Next perf steps: (1) skip_flop-style dequant needs the marlin repack interleave resolved (the 16x asymmetry finding), (2) split-K for small M, (3) deeper software pipeline. Program status: D6 done, D7 done, performance acceptance OPEN, MoE/runtime/D8 OPEN.
