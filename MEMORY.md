# MEMORY.md

## Session Log

- Project purpose: governance, plans, decisions, and verification for the vLLM inference engine, hosted as the Where room

- 2026-08-27: Adopted the host methodology at template revision 44b4c84 (case a, mode Shallow; no prior files, so the rename map is empty). The spine, rooms, recipe, tools, gate, receipts, and the upgrade baseline are in place; the reasoning is in call/0001.
- 2026-08-27: The adopted revision is the revision the host install manifest publishes as the verified set. Template HEAD is one commit later and adds the lem pronoun section, which fails the prose gate on the template's own text (dashes and arrows in its tables); until upstream lands it green, keep the adopted pin and let the upgrade ledger bring the section in.
- 2026-08-27: The gate binaries are the published release binaries placed at the recorded artifact paths: host-lint 0.18.1 and host-lifecycle 0.50.0, linux-amd64. Their sha256 match the artifact hashes in .host-software, so the installed hooks run verified against the canonical hash.
- 2026-08-27: host-prove v0.3.0 (the musl release binary) is installed at ~/.local/bin/host-prove, with ~/.local/bin added to PATH in ~/.bashrc. Reason: the gate components' obligations declare kani rungs, and software --check hazards while the re-deriver does not run.
- 2026-08-27: software --check flagged eight scripts at the vllm pin that are invoked as executables but recorded 100644 (an upstream defect at that revision). The printed remedy was applied index-only in software/vllm/main (update-index --chmod=+x), so that worktree is deliberately dirty against its pin. Never commit that change in the worktree: the pin is the audit anchor, the fix belongs upstream.
- 2026-08-27: Upgrade ledger: the stamp was migrated to baseline e4ed590 and all six pending entries were applied and recorded. Two were no-ops (the recipe already used the current keys); the reference discipline and its verify clause were already in the copied spine and the v0.50.0 binary.
- 2026-08-27: software --verify-setup requires one skill source per skill name, and the executed gate tools offer their skills from their Where-room worktrees. The duplicate tools/host-lint and tools/host-lifecycle submodules were removed, so tools/ now carries only the consumed tools (allium, specula). host-lifecycle bootstrap is the generic skill linker for this shape.
- 2026-08-27: Turing Marlin program kicked off (call/0003, plan/0002). Infrastructure: slartibardfast/vllm is the hosted fork (synced to upstream; the pin is an ancestor) and slartibardfast/agentic-vllm is the pushed host repo. Note: the gh credential on this machine is slartibardfast, not connollydavid; everything landed under slartibardfast and can be transferred later.
- 2026-08-27: Rigs. This host: two Quadro RTX 6000 (TU102, sm_75, 24 GB), driver 610.57.04, CUDA 13.3 at /opt/cuda, graphics clocks locked 1455 MHz via sudo nvidia-smi -lgc. rope (192.168.178.60, ssh dconnolly@, Arch): RTX 3060 Ti (sm_86, 8 GB), driver 610.43.02, clocks locked 1665 MHz; rope is the native Ampere reference rig. Passwordless sudo works on both.
- 2026-08-27: The incumbent Turing branch is confirmed at the pin: marlin_mma.h has six __CUDA_ARCH__ == 750 blocks (FP16 m16n8k16/m16n8k8 plus INT8 m8n8k16), the template returns early on Turing unless the activation is FP16 or INT8, and Turing is the only architecture where use_fp16_accum can be true. The fast lop3/prmt dequant is compiled on Turing too. Draft contract: plan/0002-turing-marlin-backend/marlin-contract.md.
- 2026-08-27: Rigs aligned on one stack: both run Arch with linux-lts 6.18.47-1, nvidia-open-dkms 610.57.04-1, nvidia-utils 610.57.04-1, cuda 13.3.1-1. rope was switched from the mainline kernel to linux-lts (new systemd-boot entry arch-linux-lts.conf, mainline UKI kept as fallback) and rebooted into it; driver and CUDA were upgraded from 610.43.02/13.2.1 in the same pass. Clock locks reset at reboot: re-apply -pm 1 and -lgc on each rig after every boot (rope was re-locked at 1665 post-reboot; this host holds 1455). This host still runs 6.18.46-1-lts until its next reboot; the package is already 6.18.47. Matrix: plan/0002-turing-marlin-backend/environment.md.
- 2026-08-27: The plan's top prerequisite is answered: ptxas rejects s4 as an instruction type for mma m8n8k16 on sm_75 ("incorrect instruction type"), so the INT4 tensor-core path does not exist through the sanctioned PTX interface; W4A16 on Turing dequantizes in registers. Second finding: the FP16-with-FP32-accumulate MMA on sm_75 is m16n8k8; the m16n8k16 f32-acc form requires sm_80 (the incumbent marlin_mma.h splits k16 into two k8 issues on Turing, which my first recon misread). Suite: software/vllm/sm75-marlin/turing_lab (fork branch sm75-marlin at 745d8a0); smoke run green on GPU 0 under locked clocks; real protocol (warmup, repeats, median, both cards) runs after this host's reboot to 6.18.47.
- 2026-08-27: This host rebooted to pick up linux-lts 6.18.47 on 2026-08-27 (authorized by the operator). After any reboot: the nvidia-lock-clocks.service re-applies 1455 MHz automatically on both rigs; verify with nvidia-smi --query-gpu=clocks.current.graphics and uname -r.
- 2026-08-27: First full TU102 characterization recorded (plan/0002-turing-marlin-backend/tu102-characterization.md; raw JSON on fork branch sm75-marlin at 5197c50). Headlines: FP32 accumulate costs nothing versus FP16 accumulate (both about 101 TFLOP/s at 1455 MHz), INT8 is exactly twice FP16 at 203 TOPS, every ALU kind contends with the HMMA stream (16 ops per 4 HMMAs drop it to about 24 percent), LDG reads 535 GB/s, and the W4A16 crossover hypothesis moves to M=47. Open lanes: STS, LDSM, staging chains, bank mapping, warp scaling, the M-sweep; plus a re-run of gpu0's contention section, which showed a first-section anomaly.
