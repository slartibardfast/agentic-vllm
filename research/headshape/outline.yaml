# Research outline — generated by /research (ZCode/Z.ai edition)
# Run: 2026-09-06 | supplementation agent integrated (37 sources)
topic: "Head-dimension coverage in LLM architectures and attention kernels: was D={64,128} an overfit, and what head shape should the sm_75 bridge kernel target next?"
created: "2026-09-06"

# Either/or per run (single Z.ai account), chosen at the Step 2 gate.
search_endpoints: international

items:
  - head-dim-landscape-open-models
  - qwen35-architecture-deep-dive
  - production-kernel-head-dim-coverage
  - fa2-sm75-via-bridge-feasibility
  - flashinfer-sm75-gap
  - turing-d256-perf-structure
  - overfit-coverage-precedent
  - bridge-d256-route-comparison
  - mla-qkv-dim-divergence
  - hybrid-linear-attention-coverage

execution:
  batch_size: 4          # parallel agents in /research-deep
  items_per_agent: 1     # items per agent (heterogeneous items -> 1:1)
  output_dir: ./results
